import React from 'react';
import css from './Header.module.css'

const Header = () => {
    return (
        <div id={css.header}>
            ADMIN.BIKE-BOOKING.COM
        </div>
    );
};

export default Header;
